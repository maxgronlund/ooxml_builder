!#/bin/bash
# Clean up output folder
rm -rf ../samples/pptx/*
# Run rspec
cd ../
rspec ./spec/presentation_spec.rb:26
cd spec

# Uncomment the following two lines to unpack the sample.pptx file
# ./unpack_output.sh
# ./unpack_worksheet.sh
open ../samples/pptx/sample.pptx
